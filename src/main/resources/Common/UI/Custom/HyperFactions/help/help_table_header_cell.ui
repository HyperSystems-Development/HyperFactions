// Help table header cell - bold label with left border separator

Group {
  FlexWeight: 1;

  // Left border (acts as column separator + table left border on first cell)
  Group {
    Anchor: (Width: 1, Left: 0, Top: 0, Bottom: 0);
    Background: (Color: #2a3a4a);
  }

  Label #CellText {
    Text: "";
    Style: (FontSize: 10, TextColor: #CCCCCC, RenderBold: true, Wrap: true);
    Padding: (Left: 10, Right: 8, Top: 4, Bottom: 4);
    Anchor: (Left: 1, Right: 0);
  }
}
